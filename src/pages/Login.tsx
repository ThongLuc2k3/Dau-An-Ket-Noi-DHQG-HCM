import{FormEvent,useState}from'react';import{useNavigate,Link}from'react-router-dom';import{Brand}from'../components/Brand';import{request}from'../lib/api';
export default function Login(){const[email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),nav=useNavigate();async function submit(e:FormEvent){e.preventDefault();try{const data=await request<{token:string}>('/auth/login',{method:'POST',body:JSON.stringify({email,password})});localStorage.setItem('dakn_token',data.token);nav('/admin/events')}catch(e:any){setError(e.message)}}return <main className="auth"><section className="auth-brand"><Brand light/><blockquote>“Mỗi cuộc gặp là một dấu ấn đáng trân trọng.”</blockquote></section><section className="auth-form"><Link to="/">← Trang công khai</Link><form onSubmit={submit}><p className="eyebrow">KHU VỰC QUẢN TRỊ</p><h1>Chào mừng trở lại</h1><p>Đăng nhập bằng tài khoản quản trị dùng chung hạ tầng Healthy-Skin.</p><label>Email<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} /></label><label>Mật khẩu<input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></label>{error&&<p className="error">{error}</p>}<button className="button primary">Đăng nhập →</button></form></section></main>}
