import{Link}from'react-router-dom';import{Brand}from'../components/Brand';import{demoEvent}from'../lib/events';
export default function Home(){const loggedIn=Boolean(localStorage.getItem('dakn_token'));const gate=(target:string)=>loggedIn?target:`/admin/login?next=${encodeURIComponent(target)}`;return <main className="home"><nav><Brand/>{loggedIn?<Link className="account-chip" to="/admin/events"><span>●</span>Tài khoản</Link>:<Link className="account-chip guest" to="/admin/login"><span>○</span>Đăng nhập</Link>}</nav><section className="hero"><div className="hero-copy"><p className="eyebrow">QUÀ TẶNG SỐ · ĐHQG-HCM</p><h1><span>Mỗi cuộc gặp,</span><em>một dấu ấn.</em></h1><p className="lead">Hướng camera vào thẻ kỷ niệm để khoảnh khắc và âm thanh của sự kiện sống lại ngay trước mắt.</p><div className="actions home-actions"><Link className="button primary" to={gate('/scan')}>Mở dấu ấn <span>→</span></Link><Link className="button secondary" to={gate('/admin/events')}>Kho lưu dấu ấn <span>＋</span></Link></div><p className="privacy">Camera chỉ được dùng để nhận diện thẻ. Hình ảnh từ camera không được lưu trữ.</p></div><Link to={`/e/${demoEvent.slug}`} className="memory-card"><div className="card-photo"><img src={demoEvent.thumbnail_path} alt={`Ảnh ${demoEvent.title}`}/><span className="live-dot">DẤU ẤN 2026</span></div><div><b>{demoEvent.title.toUpperCase()}</b><small>{demoEvent.partner_name} · Chạm để mở lại dấu ấn</small></div></Link></section><section className="steps"><article><span>01</span><h3>Mở camera</h3><p>Quét ảnh hoặc QR trên thẻ.</p></article><article><span>02</span><h3>Nhận ra dấu ấn</h3><p>Tìm video được liên kết.</p></article><article><span>03</span><h3>Ký ức sống lại</h3><p>Xem, tải và mở lại video.</p></article></section></main>}
