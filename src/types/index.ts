export type EventStatus='draft'|'processing'|'ready'|'active'|'inactive'|'failed';
export interface EventRecord {id:string;owner_id?:string;slug:string;title:string;partner_name:string;event_date:string;description:string;status:EventStatus;visibility:'unlisted'|'public';reference_image_path:string;thumbnail_path:string;video_path:string;target_path?:string;image_width:number;image_height:number;video_duration?:number;video_width?:number;video_height?:number;embedding?:number[];embedding_model?:string;embedding_version?:string;embedding_dimension?:number;best_video_frame_time?:number;registration_similarity?:number;feature_match_count?:number;inlier_count?:number;inlier_ratio?:number;processing_error?:string;created_at:string;updated_at:string}
export interface QualityResult {sharpness:number;brightness:number;contrast:number;keypoints:number;verdict:'pass'|'warning'|'fail'}
